Now using a delegate we parameterized function GetAllNumbersSurvivedCondition.
For that we made several shorty functions. But still there are little static functions who's 
only difference is in returning boolean. And in their names that actually we dont care about.