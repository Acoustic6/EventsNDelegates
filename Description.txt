We can simply use labdas instead of having dozens of shorty static functions written.
When we make a lambda compiler converts it to a static method behind the scenes.

the compiler converts 
i => i < 5 
to
static bool WhateverName(int number) { return number < 5; }
